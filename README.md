# 旭日图思维导图编辑器

基于ECharts的交互式旭日图思维导图编辑器，支持节点编辑、主题切换、导入导出等功能。

![项目截图](assets/screenshot-desktop.png)

## 🚀 快速部署到GitHub Pages

想要让任何人都能通过链接访问您的应用？只需几步即可部署到GitHub Pages：

### 一键部署（推荐）
1. 双击 `init-git.bat`（Windows）或运行相应命令
2. 按照提示输入GitHub仓库URL
3. 访问GitHub仓库设置 → Pages → 启用GitHub Pages
4. 等待部署完成，访问生成的链接

### 详细步骤
查看 [DEPLOY.md](DEPLOY.md) 获取完整部署指南。

### 部署后链接格式
```
https://你的用户名.github.io/仓库名/
```

### 在线演示
成功部署后，您的应用将可通过上述链接访问，无需本地服务器。

## 功能特性

### 🎨 核心功能
- **旭日图可视化**: 使用ECharts 5.x实现美观的旭日图展示
- **节点管理**: 添加、编辑、删除节点，支持无限层级
- **主题系统**: 内置暗金色和亮蓝色两种主题，一键切换
- **固定间隙**: 节点间保持固定间隙，提升视觉清晰度

### 🔧 编辑功能
- **实时编辑**: 双击节点或使用工具栏进行编辑
- **上下文菜单**: 右键点击节点显示操作菜单
- **撤销/重做**: 支持3步历史记录，Ctrl+Z/Ctrl+Y快捷键
- **拖拽调整**: 支持节点拖拽重新排序

### 📁 数据管理
- **JSON导入/导出**: 支持标准JSON格式数据交换
- **PNG导出**: 使用html2canvas导出高质量PNG图片
- **本地存储**: 自动保存到浏览器本地存储
- **示例数据**: 内置示例数据，快速上手

### 🎯 交互功能
- **节点高亮**: 点击节点高亮显示
- **缩放控制**: 支持鼠标滚轮和按钮缩放
- **搜索功能**: 快速定位节点
- **键盘快捷键**: 支持常用操作快捷键

### 📱 响应式设计
- **自适应布局**: 支持桌面、平板、手机等多种设备
- **触摸优化**: 针对触摸设备优化交互体验
- **打印友好**: 优化打印样式，支持导出为PDF

## 快速开始

### 重要提示：CORS问题
由于现代浏览器的安全限制，**直接双击打开`index.html`文件将无法正常工作**。浏览器会阻止加载本地JavaScript模块（CORS策略）。请使用以下方法之一运行应用：

### 方法一：使用HTTP服务器（推荐）

#### 使用Node.js服务器
1. 确保已安装Node.js
2. 运行以下命令：
   ```bash
   cd sunburst-mindmap
   node server.js
   ```
3. 在浏览器中打开 `http://localhost:3000`

或者双击 `start-server.bat`（Windows）文件

#### 使用Python服务器
1. 确保已安装Python 3
2. 运行以下命令：
   ```bash
   cd sunburst-mindmap
   python start-server.py
   ```
3. 在浏览器中打开 `http://localhost:8000`

### 方法二：使用开发工具
1. 安装依赖（可选）
   ```bash
   npm install
   ```

2. 启动开发服务器
   ```bash
   npm start
   ```

3. 在浏览器中打开 `http://localhost:3000`

### 方法三：配置浏览器（仅开发）
对于Chrome/Edge浏览器，可以添加启动参数禁用安全限制：
```bash
# Windows
chrome.exe --allow-file-access-from-files --disable-web-security --user-data-dir="C:/Temp"

# macOS
open -n -a /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --args --user-data-dir="/tmp/chrome_dev_test" --disable-web-security
```

**注意**：方法三仅适用于开发和测试，不推荐日常使用。

### 在线使用
部署后可直接访问 [在线演示](https://yourdomain.com/sunburst-mindmap) 使用。

## 项目结构

```
sunburst-mindmap/
├── index.html              # 主入口文件
├── src/
│   ├── core/
│   │   ├── data-model/     # 数据模型
│   │   │   ├── BaseNode.js
│   │   │   ├── TreeNode.js
│   │   │   ├── TreeManager.js
│   │   │   ├── HistoryManager.js
│   │   │   └── DataValidator.js
│   │   ├── visualization/  # 可视化引擎
│   │   │   ├── SunburstEngine.js
│   │   │   ├── LayoutCalculator.js
│   │   │   ├── GapManager.js
│   │   │   └── ColorManager.js
│   │   ├── ui-components/  # UI组件
│   │   │   ├── ToolbarController.js
│   │   │   ├── ContextMenu.js
│   │   │   └── ModalSystem.js
│   │   ├── theme/          # 主题系统
│   │   │   ├── ThemeManager.js
│   │   │   ├── DarkGoldTheme.js
│   │   │   └── LightBlueTheme.js
│   │   └── utils/          # 工具函数
│   │       ├── MathUtils.js
│   │       ├── ColorUtils.js
│   │       └── DOMUtils.js
│   └── styles/             # 样式文件
│       ├── main.css
│       ├── theme.css
│       ├── components.css
│       └── responsive.css
├── assets/                 # 静态资源
├── package.json           # npm配置
├── manifest.json          # PWA清单
└── README.md              # 项目说明
```

## 使用指南

### 基本操作
1. **添加节点**: 点击工具栏"+"按钮或右键菜单"添加子节点"
2. **编辑节点**: 双击节点或点击工具栏"编辑"按钮
3. **删除节点**: 选择节点后点击工具栏"删除"按钮或按Delete键
4. **移动节点**: 拖拽节点到新位置
5. **缩放图表**: 使用鼠标滚轮或工具栏缩放按钮

### 数据管理
- **导入JSON**: 点击工具栏"导入"按钮，选择JSON文件
- **导出JSON**: 使用右键菜单"导出为JSON"
- **导出PNG**: 点击工具栏"导出PNG"按钮
- **清空数据**: 右键菜单"清空所有数据"

### 主题切换
点击右上角"主题"按钮在暗金色和亮蓝色主题间切换。

### 快捷键
| 快捷键 | 功能 |
|--------|------|
| Ctrl+Z | 撤销 |
| Ctrl+Y | 重做 |
| Ctrl++ | 放大 |
| Ctrl+- | 缩小 |
| Ctrl+0 | 重置缩放 |
| Delete | 删除节点 |
| F1 | 显示帮助 |

## 技术栈

- **前端框架**: 原生JavaScript (ES6+)
- **可视化库**: ECharts 5.x
- **图表类型**: 旭日图 (Sunburst)
- **导出工具**: html2canvas
- **样式预处理器**: 原生CSS + CSS变量
- **构建工具**: 无构建步骤，直接使用ES6模块
- **浏览器支持**: Chrome 80+, Firefox 75+, Safari 13+, Edge 80+

## 开发指南

### 环境要求
- Node.js 14.0.0 或更高版本
- 现代浏览器（支持ES6模块）

### 开发命令
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建项目
npm run build

# 代码检查
npm run lint

# 运行测试
npm run test
```

### 代码规范
- 使用ES6+语法
- 类名使用PascalCase
- 变量名使用camelCase
- 常量使用UPPER_SNAKE_CASE
- 使用JSDoc注释

### 扩展开发
要添加新功能，可以参考现有模块结构：
1. 在 `src/core/` 下创建新模块
2. 在 `index.html` 中导入模块
3. 在 `SunburstMindMapApp` 类中集成功能
4. 添加相应的UI组件和样式

## API参考

### TreeManager
管理节点树的核心类。

```javascript
const treeManager = new TreeManager();

// 添加节点
treeManager.addNode({
  id: 'node1',
  name: '示例节点',
  parentId: 'root'
});

// 查找节点
const node = treeManager.findNode('node1');

// 删除节点
treeManager.deleteNode('node1');

// 导出数据
const json = treeManager.toJSON();
```

### SunburstEngine
ECharts旭日图渲染引擎。

```javascript
const sunburstEngine = new SunburstEngine('chart-container');

// 初始化
sunburstEngine.init();

// 设置数据
sunburstEngine.setData(treeData);

// 高亮节点
sunburstEngine.highlightNode('node1');

// 导出PNG
sunburstEngine.exportToPNG('mindmap.png');
```

### ThemeManager
主题管理类。

```javascript
const themeManager = new ThemeManager();

// 切换主题
themeManager.switchTheme('light-blue');

// 获取当前主题
const currentTheme = themeManager.getCurrentTheme();
```

## 部署

### 静态部署
将整个项目目录上传到任何静态文件服务器即可。

### GitHub Pages
1. 在GitHub仓库设置中启用GitHub Pages
2. 选择 `main` 分支和 `/ (root)` 目录
3. 访问 `https://yourusername.github.io/sunburst-mindmap-editor`

### Netlify/Vercel
1. 连接GitHub仓库
2. 构建命令留空
3. 发布目录设置为 `.`
4. 部署完成

## 浏览器兼容性

| 浏览器 | 版本 | 支持情况 |
|--------|------|----------|
| Chrome | 80+ | ✅ 完全支持 |
| Firefox | 75+ | ✅ 完全支持 |
| Safari | 13+ | ✅ 完全支持 |
| Edge | 80+ | ✅ 完全支持 |
| Opera | 67+ | ✅ 完全支持 |
| iOS Safari | 13+ | ✅ 完全支持 |
| Android Chrome | 80+ | ✅ 完全支持 |

## 性能优化

- **懒加载**: 大型节点树按需渲染
- **虚拟滚动**: 大量节点时使用虚拟滚动
- **防抖节流**: 频繁操作使用防抖节流优化
- **内存管理**: 及时清理不再使用的对象
- **GPU加速**: 使用CSS transform进行GPU加速

## 故障排除

### 常见问题

1. **图表不显示**
   - 检查ECharts是否加载成功
   - 检查浏览器控制台是否有错误
   - 确保容器元素存在且尺寸正确

2. **导入JSON失败**
   - 检查JSON格式是否正确
   - 确保JSON符合数据模型规范
   - 查看控制台错误信息

3. **导出PNG失败**
   - 检查html2canvas是否加载成功
   - 确保图表已完全渲染
   - 尝试降低导出分辨率

4. **本地存储不工作**
   - 检查浏览器是否支持localStorage
   - 确保没有使用隐私浏览模式
   - 检查存储空间是否已满

### 调试技巧
- 打开浏览器开发者工具
- 查看控制台日志
- 使用 `window.sunburstMindMapApp` 访问应用实例
- 检查网络请求

## 贡献指南

欢迎提交Issue和Pull Request！

1. Fork项目
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 致谢

- [ECharts](https://echarts.apache.org/) - 强大的可视化库
- [html2canvas](https://html2canvas.hertzen.com/) - HTML转Canvas工具
- [Font Awesome](https://fontawesome.com/) - 图标库
- 所有贡献者和用户

## 联系方式

- 项目主页: [https://github.com/yourusername/sunburst-mindmap-editor](https://github.com/yourusername/sunburst-mindmap-editor)
- 问题反馈: [GitHub Issues](https://github.com/yourusername/sunburst-mindmap-editor/issues)
- 功能建议: [GitHub Discussions](https://github.com/yourusername/sunburst-mindmap-editor/discussions)

---

**旭日图思维导图编辑器** - 让思维可视化更简单！
